package org.tearne.crosser.proto

import org.tearne.crosser.scheme.ConfigScheme
import java.nio.file.Paths
import java.io.File

object TestApp extends App{
//	val path = Paths.get("src/main/scala/example.config")
//	println(path.toUri())
//	val config = new CrosserConfig(path)
//	
//	val bank = new ChromosomeBank()
//	val rnd = new Random
//	//val dist = config.crosses.last.getDist(dists, rnd)
//	
//	val cDists = bank.getChromosomeDistributions(config.crosses("BC1"), rnd)
//	
//	//println(cDists.distributions(0))
//	println(cDists.successProbability)
//	val f = (p:Plant) => p.proportionOf(config.plants("Donor1"))
//	val samples = cDists.buildDistribution(f,rnd,0.5)
//	val writer = Writer(new File("Out.csv"))
//	writer.addColumn(samples, "PropD1")
//	writer.write()
////	println(dist.sample(rnd).chromosomes(0))
////	println(dist.sample(rnd).chromosomes(0))
////	println(dist.sample(rnd).chromosomes(0))
}