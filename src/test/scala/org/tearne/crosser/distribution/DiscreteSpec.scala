package org.tearne.crosser.distribution
import org.specs2.mutable.Specification
import org.junit.runner.RunWith
import org.specs2.runner.JUnitRunner

@RunWith(classOf[JUnitRunner])
class DiscreteSpec extends Specification{
	"Discrete distributions" should {
		"Add together, summing counts" in todo
		"Use infinity-norm for distance" in todo
		"Normalise counts" in todo
		"Override equals and hashcode" in todo
		"Have factory construction" in todo
	}
}