package org.tearne.crosser.distribution

import org.junit.runner.RunWith
import org.specs2.runner.JUnitRunner
import org.specs2.mutable.Specification

@RunWith(classOf[JUnitRunner])
class ChromosomeDistributionSpec extends Specification{
	"ChromosomeDistribution" should {
		"determine success probability given inputs" in todo
		"have ++ to generate augmented distribution" in todo
		"support distanceTo" in todo
		"generate individual plants" in todo
		"have companion object for creating empty distribution" in todo
	}
}