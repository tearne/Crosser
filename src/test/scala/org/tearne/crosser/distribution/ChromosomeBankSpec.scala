package org.tearne.crosser.distribution

import org.specs2.mutable.Specification
import org.junit.runner.RunWith
import org.specs2.runner.JUnitRunner

@RunWith(classOf[JUnitRunner])
class ChromosomeBankSpec extends Specification{
	"ChromosomeBancSpec" should {
		"return chromosome distribution if in the bank" in todo
		"use chromosome distribution builder if not already in bank" in todo
	}
}