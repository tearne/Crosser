package org.tearne.crosser.plant

import org.specs2.mutable.Specification
import org.junit.runner.RunWith
import org.specs2.runner.JUnitRunner

@RunWith(classOf[JUnitRunner])
class RootPlantSpec extends Specification{
	"RootPlant" should {
		"be a concrete plant" in todo
		"be crossable" in todo
		"have chromosomes entirely containing references to self" in todo
		"have value based hashcode and equals" in todo
	}
}