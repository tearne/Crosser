package org.tearne.crosser.plant

import org.specs2.mutable.Specification
import org.junit.runner.RunWith
import org.specs2.runner.JUnitRunner

@RunWith(classOf[JUnitRunner])
class ChromosomeSpec extends Specification {
	"Chromosome" should {
		"throw exception if left and right tid differ in length" in todo
		"give the proportion of a plant present" in todo
		"have value based hashcode and equals" in todo
	}
}