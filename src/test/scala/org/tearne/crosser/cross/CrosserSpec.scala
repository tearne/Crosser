package org.tearne.crosser.cross

import org.junit.runner.RunWith
import org.specs2.runner.JUnitRunner
import org.specs2.mutable.Specification

@RunWith(classOf[JUnitRunner])
class CrosserSpec extends Specification{
	"Crosser" should {
		"generate offspring plants from concrete plants" in todo
		"throw exception if left and right are different species" in todo
	}
}